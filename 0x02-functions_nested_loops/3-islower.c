#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	int c = 'a'

	for (c = 'a'; c <= 'z'; c++)
	{
		if (c == 'c')
		{
			return (1);
		}
		else
		{
			retrun (0);
		}
	}
}
