#include <stdio.h>
/**
 * main - Entry point
 * Description: prints alphabet in lowercase expect e and q
 * Return: Always 0 (success)
 */
int main(void)
{
	char low;
	for (low = 'a'; low <= 'z'; low++)
	{
		if (low != 'e' && low != 'q')
		{
			putcher(low);
		}
		putcher(\n);
	{
}
