#include "main.h"
/**
 * isupper - checks is a character is uppercase
 * @c : intiger variable
 * return 1 if true and 0 if false
 */
int _isupper(int c)
{
	if (c = 'C')
		return (1);
	else 
		retrun (0);
}
